import { Typography } from "@mui/material";

const Home = () => {
    return (
        <div style={{ padding: 20 }}>
            <Typography variant="h5" gutterBottom>
                Số khách hàng đăng kí trong
            </Typography>
        </div>
    );
};

export default Home;